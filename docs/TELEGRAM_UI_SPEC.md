# Спецификация Telegram UI (меню vs inline)

Документ описывает, какие кнопки должны быть **в постоянном меню** (reply keyboard / menu) и какие — **инлайн** (inline keyboard) внутри конкретных сообщений, чтобы диалоги были быстрыми и не «захламляли» чат.

## 1) Термины и принципы

### Типы кнопок

- **Меню (ReplyKeyboardMarkup)** — постоянные большие кнопки под полем ввода. Подходит для верхнеуровневой навигации.
- **Инлайн (InlineKeyboardMarkup)** — кнопки под конкретным сообщением. Подходит для выбора значения/действий именно в контексте этого сообщения.

### Общие принципы выбора

1. **Навигация и разделы** — в меню.
   - Примеры: «Записаться», «Мои записи», «Помощь», «Заявки (для менеджера)».
2. **Выбор значения** (дата/время/длительность/кабинет/аппарат) — инлайн.
3. **Действие над конкретной заявкой** — инлайн в карточке заявки.
   - Примеры: «Подтвердить», «Отклонить», «Перенести», «Сменить аппарат».
4. **“Назад/Отмена” внутри мастера** — инлайн.
5. **Редкие/опасные операции** (массовые действия, удаление, блокировки) — только из менеджерского меню и с подтверждением инлайн.

### Callback-data (рекомендации)

- Формат: `scope:entity:action:id[:arg]`
  - Примеры: `u:book:pick_date`, `mgr:booking:approve:123`.
- Не хранить PII в `callback_data` (ФИО/телефон/комментарии). Хранить в сессии/БД.
- Везде иметь кнопку «↩️ Назад» и «✖️ Отмена» в мастере.

---

## 1.1) Отказоустойчивость интеграции с Jr (Bot 1)

CRM-бот зависит от Jr-бота в части списка аппаратов и проверки/резерва аппарата.

Важно разделять два типа сбоев:

- **Jr не отвечает / отвечает с ошибкой**: таймауты, сетевые ошибки, 5xx, неверный JSON.
- **Jr отвечает корректно, но сообщает “недоступно”**: аппарат реально занят/нельзя забронировать.

Ниже — правила поведения, чтобы пользователь не “застревал”, а менеджер понимал риск.

### A) Загрузка списка аппаратов (экран выбора аппарата)

Сценарии:

1) **Успех** — показываем список аппаратов + `Без аппарата`.
2) **Сбой/таймаут/невалидный ответ** — показываем минимум:
   - `Без аппарата`
   - (опционально) `🔁 Повторить загрузку`

Текст предупреждения (коротко):
> ⚠️ Не удалось получить список аппаратов из внешней системы. Можно продолжить без выбора аппарата — менеджер уточнит и подтвердит запись.

### B) Проверка доступности аппарата (перед подтверждением)

Сценарии:

1) **Ответ: доступно** — продолжаем без предупреждения.
2) **Ответ: недоступно** — не даём продолжить с этим аппаратом:
   - возвращаем на выбор аппарата
   - предлагаем `Без аппарата`
3) **Сбой/таймаут/невалидный ответ** — разрешаем продолжить, но помечаем заявку как “аппарат не подтверждён”:
   - пользователь видит предупреждение в шаге подтверждения
   - менеджеры в карточке заявки видят пометку “⚠️ Jr недоступен — аппарат не подтверждён”

Важно: при “сбое” мы не утверждаем, что аппарат доступен/недоступен.

### C) Резерв аппарата в Jr (если/когда это делается)

Рекомендуемое правило:

- **Резерв аппарата = “жёсткая операция”** и должна выполняться при действии менеджера `✅ Подтвердить` (или в конце пользовательского мастера, если выбран другой процесс).

Если при резерве:

1) **Jr отвечает “недоступно” (409/конфликт)**:
   - НЕ переводить заявку в `APPROVED`
   - предложить менеджеру: `🔧 Сменить аппарат` или `✍️ Попросить доработку`
2) **Jr не отвечает/ошибка/невалидный ответ**:
   - НЕ переводить заявку в `APPROVED`
   - показать менеджеру: “Jr недоступен, попробуйте позже” + `🔁 Повторить`

### D) Каким текстом говорить пользователю

Чтобы избежать ложных гарантий:

- Не писать “аппарат забронирован”, если Jr недоступен.
- Писать: “аппарат будет подтверждён менеджером” / “внешняя система недоступна, менеджер уточнит аппарат при звонке”.

### E) Технические требования (клиент Jr API)

- Таймаут на запросы к Jr в пользовательском диалоге: 2–5 секунд.
- Любой не-2xx ответ = ошибка интеграции (не трактовать как “недоступно”, пока явно не обработано).
- Невалидный JSON/пустой ответ/несоответствие схеме = “ошибка интеграции”.
- Все операции к Jr должны быть идемпотентны по `external_booking_id`.

---

## 2) CRM-бот (кабинеты, почасово): пользователь

### Постоянное меню (для обычного пользователя)

Рекомендуемый набор кнопок:

- `🗓 Записаться`
- `📌 Мои записи`
- `🔔 Напоминания`
- `ℹ️ Помощь`

Примечания:
- «🔔 Напоминания» ведёт в экран настроек (включить/выключить) и краткое объяснение.
- «📌 Мои записи» показывает список активных/будущих записей, каждая — отдельной карточкой с инлайн-действиями.

### Мастер “Записаться” (инлайн)

#### Шаг 0: старт
Сообщение: кратко, что будет дальше, и предупреждение про подтверждение менеджером.

Инлайн:
- `Начать`
- `Отмена`

#### Шаг 1: выбор кабинета (если кабинетов > 1)
Инлайн:
- `🚪 Кабинет 1`, `🚪 Кабинет 2`, ...
- `↩️ Назад`, `✖️ Отмена`

#### Шаг 2: выбор даты
Вариант A (если календарь уже реализован): инлайн-календарь.

Вариант B (простая реализация):
- `Сегодня`, `Завтра`, `Выбрать дату…`
- `↩️ Назад`, `✖️ Отмена`

#### Шаг 3: выбор времени (слоты)
Инлайн:
- список свободных слотов на выбранную дату (по 6–10 кнопок на экран)
- `◀️ Раньше`, `Позже ▶️` (пагинация)
- `↩️ Назад`, `✖️ Отмена`

#### Шаг 4: выбор длительности
Инлайн:
- `30 мин`, `60 мин`, `90 мин`, `120 мин` (опции из доступных по расписанию)
- `↩️ Назад`, `✖️ Отмена`

#### Шаг 5: выбор аппарата (интеграция с Jr)
Инлайн:
- список доступных аппаратов (по 6–10 на экран)
- `↩️ Назад`, `✖️ Отмена`

#### Шаг 6: ввод ФИО
Это **ввод текстом**, не кнопкой.

Инлайн под подсказкой:
- `↩️ Назад`, `✖️ Отмена`

#### Шаг 7: подтверждение заявки
Сообщение: “Проверьте данные” + предупреждение, что требуется подтверждение менеджера.

Инлайн:
- `✅ Отправить заявку`
- `✏️ Изменить время`
- `✏️ Изменить длительность`
- `✏️ Изменить аппарат`
- `✏️ Изменить ФИО`
- `✖️ Отмена`

### “Мои записи” (карточки + инлайн)

Для каждой записи:
- `❌ Отменить` (если разрешено правилами)
- `💬 Написать комментарий` (если хотим давать пользователю канал уточнений)

Отмена — через подтверждение:
- `Да, отменить`
- `Нет`

---

## 3) CRM-бот: менеджер

### Постоянное меню (для менеджера)

Рекомендуемый набор кнопок (можно 2 ряда):

- `📥 Заявки`
- `📅 Расписание`
- `🔎 Поиск`
- `➕ Создать заявку`
- `📊 Отчёты`
- `⚙️ Настройки`

Пояснения:
- «📥 Заявки» — очередь (PENDING/на доработке), плюс быстрые фильтры.
- «📅 Расписание» — сегодня/завтра + выбор даты.
- «🔎 Поиск» — поиск по ID, дате, ФИО.
- «➕ Создать заявку» — ручное создание (см. ниже).

### Карточка заявки (инлайн-действия)

В карточке заявки менеджер видит: дата/время, кабинет, длительность, аппарат, ФИО клиента, статус, комментарии.

Инлайн (для PENDING):
- `✅ Подтвердить`
- `❌ Отклонить`
- `📝 На доработку`
- `🔄 Перенести`
- `⏱ Длительность`
- `🔧 Аппарат`
- `💬 Комментарий`

Инлайн (для APPROVED):
- `🔄 Перенести`
- `🔧 Аппарат`
- `⏱ Длительность`
- `❌ Отменить`
- `💬 Комментарий`

Для “опасных” действий всегда дополнительное подтверждение:
- `Да, выполнить`
- `Нет`

### Менеджер создаёт заявку сам (➕ Создать заявку)

Цель: менеджер может занять слот вручную (например, запись пришла по телефону) и оставить комментарий.

Мастер (рекомендуемый порядок):
1. Кабинет (инлайн)
2. Дата (инлайн)
3. Время старта (инлайн свободные слоты)
4. Длительность (инлайн)
5. Аппарат (инлайн, как в пользовательском мастере)
6. ФИО клиента (текст)
7. Комментарий менеджера (текст; можно пустой)
8. Привязка к пользователю (инлайн):
   - `Привязать по Telegram ID` → менеджер вводит числом
   - `Без привязки` (останется «ручная запись»)
9. Итоговое подтверждение (инлайн):
   - `✅ Создать`
   - `✏️ Изменить…` (пункты)
   - `✖️ Отмена`

Поведение после создания:
- Запись сразу создаётся в статусе:
  - вариант A: `APPROVED` (если менеджер сам создаёт, логично считать подтверждённой)
  - вариант B: `PENDING`, но сразу можно нажать `✅ Подтвердить`
- Если есть привязка к Telegram ID, пользователю отправляется уведомление с деталями.
- Комментарий менеджера хранится в заявке (желательно как “internal note” + опционально “comment for user”).

---

## 4) Jr-бот (аппараты, на день): пользователь и менеджер

Если Jr используется как “самостоятельный” бот для бронирования аппаратов на день, UI можно упростить:

### Пользовательское меню
- `🗓 Забронировать аппарат`
- `📌 Мои брони`
- `🔔 Напоминания`
- `ℹ️ Помощь`

### Мастер бронирования (инлайн)
- Выбор даты (инлайн)
- Выбор аппарата (инлайн)
- Подтверждение (инлайн)

### Менеджерские действия
- Инлайн в карточке брони: `✅ Подтвердить`, `❌ Отклонить`, `🔄 Перенести`, `🔧 Аппарат`, `💬 Комментарий`.

---

## 5) Минимальный “MVP-набор” кнопок

Если нужно стартовать быстро:

- Пользователь CRM: меню `Записаться / Мои записи / Помощь`.
- В мастере: инлайн `Назад / Отмена` + выборы значений.
- Менеджер: меню `Заявки / Создать заявку / Расписание`.
- В карточке заявки: инлайн `Подтвердить / Отклонить / Перенести / Комментарий`.
