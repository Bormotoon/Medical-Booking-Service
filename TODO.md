# TODO: Medical Booking Service

> Подробный план разработки на основе [features.md](features.md)

---

## Легенда статусов

- [ ] Не начато
- [~] В работе
- [x] Завершено

---

## 0. Подготовка инфраструктуры

### 0.1 Общая архитектура монорепо
- [x] **Определить структуру проекта** ✅ (13.01.2026)
  - Описание: Унифицировать структуру `bronivik_jr` (Бот 1 — аппараты) и `bronivik_crm` (Бот 2 — кабинеты).
  - Рекомендации:
    - Выделить общие модули (напоминания, аудит, access control) в `shared/` или `common/`.
    - Использовать единую схему конфигурации (`.env`, `config.yaml`).
    - Документировать API-контракты между ботами (OpenAPI/Swagger).

### 0.2 База данных
- [x] **Спроектировать схему БД для обоих ботов** ✅ (13.01.2026)
  - Описание: Таблицы для заявок, пользователей, аппаратов, кабинетов, расписаний, настроек напоминаний, чёрного списка, менеджеров.
  - Рекомендации:
    - Использовать PostgreSQL или SQLite (для MVP).
    - Добавить поле `created_at` для политики удаления (31 день).
    - Предусмотреть soft-delete или архивирование перед жёстким удалением.

---

## I. ОБЩИЕ ТРЕБОВАНИЯ (ОБА БОТА)

### 1.1 Система напоминаний о бронировании

#### 1.1.1 Автоматическая отправка напоминаний
- [x] **Реализовать планировщик напоминаний** ✅ (13.01.2026)
  - Описание: За 24 часа до начала брони отправлять пользователю сообщение с информацией о бронировании.
  - Рекомендации:
    - Использовать `cron`-задачу или встроенный scheduler (APScheduler для Python, `time.Ticker` для Go).
    - Запускать проверку каждые 15-30 минут, искать заявки с `start_time - now() <= 24h` и `reminder_sent = false`.
    - После отправки ставить флаг `reminder_sent = true`.

#### 1.1.2 Кнопка «Настройки напоминаний»
- [x] **Добавить inline-кнопку в напоминание** ✅ (13.01.2026)
  - Описание: Вместе с текстом напоминания отправлять кнопку для перехода в настройки.
  - Рекомендации:
    - Использовать `InlineKeyboardMarkup` (Telegram Bot API).
    - Callback: `settings_reminders`.

#### 1.1.3 Управление настройками напоминаний
- [x] **Создать таблицу `user_settings`** ✅ (13.01.2026)
  - Поля: `user_id`, `reminders_enabled` (bool, default=true).
- [x] **Реализовать UI для вкл/выкл напоминаний** ✅ (13.01.2026)
  - Описание: В разделе «Мои заявки» добавить переключатель.
  - Рекомендации:
    - Использовать inline toggle-кнопку: 🔔 Напоминания: ВКЛ / ВЫКЛ.
    - При нажатии — инвертировать значение в БД и обновить текст кнопки.

---

### 1.2 Ежемесячный аудит и экспорт данных

#### 1.2.1 Генерация XLS-отчёта
- [x] **Реализовать задачу экспорта** ✅ (13.01.2026)
  - Описание: 1-го числа каждого месяца формировать Excel-файл со всеми таблицами БД.
  - Реализовано:
    - Модуль `shared/audit/` с интерфейсами и сервисом.
    - `shared/audit/excel.go` — ExcelWriter на базе excelize.
    - `shared/audit/service.go` — AuditScheduler с автозапуском 1-го числа.
    - `bronivik_jr/internal/database/audit.go` — TableExporter для Бота 1.
    - `bronivik_crm/internal/db/audit.go` — TableExporter для Бота 2.
    - Имя файла: `{Месяц}_{Год}.xlsx` на русском языке.

#### 1.2.2 Отправка отчёта менеджеру
- [x] **Отправить файл в чат менеджера** ✅ (13.01.2026)
  - Описание: После генерации отправить документ через Telegram Bot API.
  - Реализовано:
    - Интерфейс `TelegramSender` в `shared/audit/interfaces.go`.
    - Метод `sendReportToManagers()` в `AuditScheduler`.
    - Получение менеджеров через `ManagerProvider` interface.

#### 1.2.3 Автоудаление старых заявок
- [x] **Реализовать очистку данных старше 31 дня** ✅ (13.01.2026)
  - Описание: После экспорта (или отдельной задачей) удалять записи.
  - Реализовано:
    - Интерфейс `DataCleaner` с методом `DeleteOldBookings(ctx, days int)`.
    - Вызов после успешного экспорта в `runMonthlyAudit()`.
    - Логирование количества удалённых записей через zerolog.

---

### 1.3 Система управления доступом

#### 1.3.1 Чёрный список (blocklist)
- [ ] **Создать таблицу `blocked_users`**
  - Поля: `user_id`, `blocked_at`, `reason` (optional).
- [ ] **Проверять пользователя при любом действии**
  - Описание: Если `user_id` в чёрном списке — отклонять запрос с сообщением.
  - Рекомендации:
    - Middleware/decorator на уровне обработчика команд.

#### 1.3.2 Список менеджеров
- [ ] **Создать таблицу `managers`**
  - Поля: `user_id`, `chat_id`, `name`, `added_at`.
- [ ] **Реализовать проверку прав**
  - Описание: Админ-команды доступны только пользователям из `managers`.
  - Рекомендации:
    - Декоратор `@manager_only` или middleware.

---

## II. БОТ 1: Бронирование аппаратов (`bronivik_jr`)

### 2.1 Управление аппаратами

#### 2.1.1 «Вечная аренда» аппаратов
- [ ] **Добавить поле `permanent_reserved` в таблицу `devices`**
  - Тип: `boolean`, default=false.
- [ ] **Реализовать команду менеджера для резервирования**
  - Описание: Менеджер отмечает аппарат как «забронированный навсегда» для нужд Бота 2.
  - Рекомендации:
    - Команда: `/reserve_device <device_id>` или inline-меню.
    - Такие аппараты исключаются из обычного бронирования, но доступны через API.

---

### 2.2 API для интеграции с Ботом 2

#### 2.2.1 Эндпоинт `GET /api/devices`
- [ ] **Реализовать HTTP-сервер (если ещё нет)**
  - Рекомендации:
    - Go: `net/http` + `gorilla/mux` или `chi`.
    - Python: `FastAPI` или `Flask`.
- [ ] **Реализовать endpoint**
  - Описание: Возвращает JSON-список доступных аппаратов.
  - Response:
    ```json
    {
      "devices": [
        {"id": 1, "name": "Аппарат A", "available": true},
        {"id": 2, "name": "Аппарат B", "available": false}
      ]
    }
    ```
  - Рекомендации:
    - Учитывать `permanent_reserved` и текущие активные брони.
    - Добавить query-параметры для фильтрации по дате/времени.

#### 2.2.2 Эндпоинт `POST /api/book-device`
- [ ] **Реализовать endpoint бронирования**
  - Описание: Бот 2 отправляет запрос на бронирование аппарата.
  - Request:
    ```json
    {
      "device_id": 1,
      "date": "2026-01-15",
      "start_time": "14:00",
      "end_time": "15:30",
      "external_booking_id": "crm-12345"
    }
    ```
  - Response:
    ```json
    {"success": true, "booking_id": 789}
    ```
    или
    ```json
    {"success": false, "error": "Device not available"}
    ```
  - Рекомендации:
    - Проверять пересечение временных интервалов.
    - Использовать транзакции для атомарности.
    - Добавить API-ключ или JWT для авторизации между сервисами.

---

## III. БОТ 2: Бронирование кабинетов (`bronivik_crm`)

### 3.1 Гибкое управление расписанием

#### 3.1.1 Расписание по умолчанию
- [ ] **Создать таблицу `default_schedule`**
  - Поля: `day_of_week` (0-6), `start_time`, `end_time`, `lunch_start`, `lunch_end`.
  - Значения по умолчанию: 10:00–22:00, обед отсутствует.

#### 3.1.2 Особое расписание на конкретные даты
- [ ] **Создать таблицу `special_schedule`**
  - Поля: `date`, `start_time`, `end_time`, `lunch_start`, `lunch_end`, `is_day_off` (bool).
- [ ] **Реализовать функцию получения расписания на дату**
  - Логика: Сначала проверить `special_schedule`, затем `default_schedule`.

#### 3.1.3 Команды менеджера для управления расписанием
- [ ] **Изменение дефолтного расписания**
  - Команда: `/set_default_schedule` → диалог с выбором дня недели, времени начала/конца, обеда.
- [ ] **Установка особого расписания**
  - Команда: `/set_special_day 2026-01-20` → выходной / сокращённый день.
- [ ] **Валидация: запрет изменения при активных заявках**
  - Описание: Перед изменением проверять, нет ли неперенесённых заявок на эту дату/время.
  - Рекомендации:
    - Если есть — показать список заявок и предложить сначала перенести/отменить.

---

### 3.2 Диалог бронирования для пользователя

#### 3.2.1 Сбор данных клиента
- [ ] **Реализовать пошаговый диалог (FSM)**
  - Шаги:
    1. Запрос ФИО.
    2. Выбор даты (inline-календарь или ввод).
    3. Выбор времени начала (из доступных слотов).
    4. Выбор длительности (количество слотов по 30 мин).
    5. Выбор аппарата (список из API Бота 1).
    6. Подтверждение.
  - Рекомендации:
    - Использовать FSM (Finite State Machine): `aiogram` FSM, `python-telegram-bot` ConversationHandler, или собственная реализация.

#### 3.2.2 Слоты по 30 минут
- [ ] **Генерировать доступные слоты на основе расписания**
  - Описание: Разбить рабочее время на 30-минутные интервалы, исключить обед и уже занятые.
  - Рекомендации:
    - Функция `get_available_slots(date, room_id)` → список `[(10:00, 10:30), (10:30, 11:00), ...]`.

#### 3.2.3 Выбор нескольких последовательных слотов
- [ ] **Позволить выбрать 1–N слотов подряд**
  - Описание: Пользователь выбирает начало и количество слотов (или конечное время).
  - Рекомендации:
    - Inline-кнопки: «30 мин», «1 час», «1.5 часа», «2 часа», «Другое».
    - Проверять, что все слоты свободны.

#### 3.2.4 Интеграция с API Бота 1
- [ ] **Получить список аппаратов через `GET /api/devices`**
  - Показать пользователю для выбора.
- [ ] **Забронировать аппарат через `POST /api/book-device`**
  - Вызывать после выбора, до финального подтверждения.
  - При неудаче — предложить другой аппарат или время.

#### 3.2.5 Проверка доступности кабинета
- [ ] **Проверить, свободен ли кабинет в выбранное время**
  - SQL: искать пересечения по `room_id`, `date`, `start_time`, `end_time`.

#### 3.2.6 Создание заявки со статусом «ожидает подтверждения»
- [ ] **Создать запись в таблице `bookings`**
  - Статус: `pending`.
  - Время считается «условно занятым» (блокирует слоты для других).

#### 3.2.7 Информационное предупреждение
- [ ] **Показать disclaimer перед подтверждением**
  - Текст: «Окончательное подтверждение зависит от звонка менеджера и доступности аппаратов».

---

### 3.3 Расширенный функционал менеджера

#### 3.3.1 Просмотр заявок
- [ ] **Команда `/bookings` — список всех заявок**
  - Фильтры: по статусу, дате, пользователю.
  - Рекомендации:
    - Пагинация (по 10 заявок на страницу).
    - Inline-кнопки для навигации и действий.

#### 3.3.2 Редактирование заявки
- [ ] **Перенос на другую дату/время**
  - Описание: Изменить `date`, `start_time`, `end_time`.
  - Рекомендации:
    - Проверить доступность нового слота.
    - Уведомить пользователя о переносе.
    - Обновить бронь аппарата в Боте 1 (отменить старую, создать новую).

- [ ] **Изменение длительности (количества слотов)**
  - Описание: Увеличить или уменьшить `end_time`.
  - Рекомендации:
    - При увеличении — проверить, свободны ли дополнительные слоты.

- [ ] **Изменение аппарата**
  - Описание: Выбрать другой аппарат из доступных.
  - Рекомендации:
    - Освободить старый аппарат в Боте 1.
    - Забронировать новый.

#### 3.3.3 Управление статусами заявки
- [ ] **Согласовать (одобрить)**
  - Изменить статус на `approved`.
  - Уведомить пользователя: «Ваша заявка подтверждена».

- [ ] **Отклонить**
  - Изменить статус на `rejected`.
  - Освободить слоты кабинета и аппарата.
  - Уведомить пользователя с причиной (опционально).

- [ ] **Вернуть на доработку**
  - Изменить статус на `needs_revision`.
  - Отправить пользователю комментарий с просьбой изменить детали.
  - Пользователь может отредактировать и повторно отправить.

---

### 3.4 Конфигурация и интеграции

#### 3.4.1 Настройка количества кабинетов
- [ ] **Параметр конфигурации `ROOMS_COUNT`**
  - Описание: Задаётся при запуске/деплое.
  - Рекомендации:
    - Переменная окружения или config-файл.
    - При старте создавать записи в таблице `rooms` (если их нет).

#### 3.4.2 Интеграция с Google Таблицами
- [ ] **Настроить Google Sheets API**
  - Описание: Выгрузка/синхронизация данных (детали уточняются).
  - Рекомендации:
    - Использовать сервисный аккаунт.
    - Библиотеки: `gspread` (Python), `google/api` (Go).
    - Возможные сценарии:
      - Ежедневная/еженедельная выгрузка заявок.
      - Real-time синхронизация (webhook или polling).

#### 3.4.3 Интеграция с Ботом 1
- [ ] **Настроить URL и авторизацию для API Бота 1**
  - Переменные: `BOT1_API_URL`, `BOT1_API_KEY`.
  - Рекомендации:
    - Health-check при старте: `GET /api/health` или `GET /api/devices`.
    - Graceful degradation: если Бот 1 недоступен — показать сообщение пользователю.

---

## IV. Тестирование и документация

### 4.1 Тесты
- [ ] **Unit-тесты для бизнес-логики**
  - Генерация слотов, проверка пересечений, валидация расписания.
- [ ] **Integration-тесты для API**
  - Тестировать `GET /api/devices`, `POST /api/book-device`.
- [ ] **E2E-тесты для ботов**
  - Эмуляция диалога с пользователем.

### 4.2 Документация
- [ ] **README для каждого бота**
  - Установка, настройка, запуск.
- [ ] **OpenAPI/Swagger для API Бота 1**
  - Описание эндпоинтов, примеры запросов/ответов.
- [ ] **Инструкция для менеджера**
  - Список команд, сценарии использования.

---

## V. Деплой и DevOps

- [ ] **Docker-образы для обоих ботов**
- [ ] **docker-compose для локальной разработки**
- [ ] **CI/CD pipeline (GitHub Actions)**
  - Линтинг, тесты, сборка, деплой.
- [ ] **Настройка продакшен-окружения**
  - Переменные окружения, секреты, база данных.

---

## Приоритеты (рекомендуемый порядок)

1. **MVP Бота 1**: таблица аппаратов, базовые CRUD, API `/api/devices` и `/api/book-device`.
2. **MVP Бота 2**: диалог бронирования, таблица заявок, интеграция с API Бота 1.
3. **Общие модули**: напоминания, access control.
4. **Менеджерский функционал**: управление заявками, расписанием.
5. **Аудит и экспорт**: XLS-отчёты, автоочистка.
6. **Интеграции**: Google Sheets.
7. **Тесты, документация, DevOps**.

---

*Последнее обновление: 13 января 2026*
